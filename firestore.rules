rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All database access goes through trusted Next.js server actions.
    // The Admin SDK uses application credentials and bypasses client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
