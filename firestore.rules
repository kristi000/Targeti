rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never accesses Firestore directly. Authenticated server
    // actions use the Admin SDK and IAM, so all client SDK access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
